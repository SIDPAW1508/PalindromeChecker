# UC9: Recursive Palindrome Checker

## Description
This feature implements a palindrome check using **recursion**.  
It compares characters from the outer ends of the string, moving inward recursively.

## How It Works
- Compare characters at `start` and `end` indices.
- If they match, recursively check the substring between them.
- Stop when all characters are matched or a mismatch is found.
